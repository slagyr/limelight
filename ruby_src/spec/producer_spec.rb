require File.expand_path(File.dirname(__FILE__) + "/spec_helper")

describe Limelight::Producer do
  
  before(:each) do
  end
  
  it "should open a simple page" do
    # loader = make_mock("page loader")
    # Limelight::FilePageLoader.should_receive(:with_root).with("simple")
    # loader.should_receive(:path_to).with("blocks.rb").and_return("")
    # 
    # Limelight::Producer.open("simple")
    
  end
  
end
